# ミセバ — 制作ルール（共通メモリ）

このファイルは Claude Code が**毎回自動で読む**共通ルールです。
新しい試作サイトを作るときは、必ずこのルールに従ってください。
店舗固有の情報（店名・住所・電話・メニュー）はここには書きません。

---

## 0. ミセバとは

小規模事業者（屋根工事・水道・工務店・電気工事・整体院など）に対して、
**先に試作サイトを作って見せてから提案する**個人事業の屋号です。

- 屋号：**ミセバ**（英字表記：`Miseba`）
- 由来：「見せる」＋「店（ミセ）」＋「場」
- **法人ではありません。**「株式会社」「合同会社」を名乗ってはいけません（会社法違反）。
  名乗り方は `brand/BRAND.md` を参照。

---

## 1. 絶対に守る制約（違反したらやり直し）

### 1-1. 事実の捏造を禁止する

試作サイトに書いてよいのは、**Googleマップ上の公開情報として渡されたものだけ**です。
以下は情報が渡されていない限り**絶対に書かない**。プレースホルダーも置かない。

- 創業年 / 営業年数 / 施工実績数 / 従業員数
- 保有資格 / 認定 / 受賞歴 / 加盟団体
- 料金 / 価格 / 割引 / キャンペーン
- 「地域No.1」「実績多数」「安心保証」など裏付けのない優良誤認表現
- 対応エリア（住所から明らかな市区町村を超える範囲）

情報がない項目は、**そのセクションごと省略**してください。空欄で残さない。

### 1-2. 相手の著作物を使わない

- 相手のロゴ・看板写真・施工写真は**一切使わない**（著作権／不正競争防止法）
- 写真は **Unsplash のみ**（商用利用可・許可不要）。業種に合う汎用写真を使う
- 口コミは「引用」として短く扱い、全文転載しない

### 1-3. AI試作であることを明示する

全ページのフッターに、以下を**必ず**入れる。

```html
<p class="miseba-note">
  このページは、Googleマップ上の公開情報をもとに
  <strong>ミセバ</strong>が作成した試作サイトです。
  内容の追加・修正が可能です。不要な場合はご連絡ください。
</p>
```

### 1-4. 検索エンジンに載せない

全ページの `<head>` に必ず：

```html
<meta name="robots" content="noindex, nofollow">
```

---

## 2. デザインルール

### 2-1. 業種別カラー

| 業種 | メイン | アクセント | 背景 |
|---|---|---|---|
| 屋根工事・外装 | `#1B4965` (深い青) | `#F4A261` (オレンジ) | `#F8F9FA` |
| 水道・設備 | `#0077B6` (水色系) | `#00B4D8` | `#F8FBFD` |
| 工務店・建築 | `#3E2723` (木の茶) | `#8D6E63` | `#FAF7F2` |
| 電気工事 | `#264653` | `#E9C46A` | `#F8F9FA` |
| 整体・治療院 | `#00897B` (ティール) | `#4DB6AC` | `#E8F5E9` |
| 飲食 | `#2B2B2B` | `#C1440E` | `#FFFFFF` |

指定がなければ**屋根工事の配色**をデフォルトにする。

### 2-2. タイポグラフィ

- フォント：Google Fonts の **Noto Sans JP**（400 / 700）
- 本文：`16px` 以上（スマホでも縮小しない）
- 見出し：`clamp()` で可変（例 `clamp(1.75rem, 5vw, 2.75rem)`）
- 行間：本文 `1.8`、見出し `1.4`

### 2-3. レイアウト

- **モバイルファースト**。320px〜1920px で崩れないこと
- コンテンツ最大幅：`1100px`、左右パディング `20px`
- セクション上下余白：スマホ `56px` / PC `88px`
- タップ領域は最低 **48px × 48px**

### 2-4. モバイル固定CTAバー（★重要★）

対象業種は「困ってすぐ電話する」客層なので、**スマホでは画面下部に固定バー**を必ず置く。

```
[ 📞 電話する ]  [ お問い合わせ ]
```

- 電話は `tel:` リンク（ワンタップ発信）
- PCでは非表示、スマホ（`max-width: 768px`）でのみ表示
- 固定バーの高さ分、`body` の下部に `padding-bottom` を確保する

### 2-5. アニメーション

上品で控えめに。派手な演出は禁止。

- スクロール時のフェードイン＋下から上へのスライド（`IntersectionObserver` 使用）
- ボタンホバー：わずかな浮き上がり（`translateY(-2px)` ＋ シャドウ）
- 所要時間は `0.3s`〜`0.5s`、`ease-out`
- `prefers-reduced-motion` を尊重して無効化できるようにする

---

## 3. コーディング規約

- **1ページ = 1つのHTMLファイル**。CSS/JSはインラインで埋め込む（配布・プレビューを簡単にするため）
- 外部依存は Google Fonts と Unsplash の画像URLのみ。CDNのJSライブラリは使わない
- セマンティックHTML（`<header> <main> <section> <footer>`）
- 見出しは `h1` → `h2` → `h3` の階層を守る。飛ばさない
- 画像には必ず意味のある `alt` を書く
- 色のコントラスト比は本文 4.5:1 以上
- 日本語。`lang="ja"`

---

## 4. サイト構成

### 試作段階は「1ページ」が正解（★重要★）

営業で送る試作サイトは **`index.html` の1ページだけ**にする。複数ページにしない。

理由：
- 相手は初対面。**4ページ読ませる関係性ではない**。1画面で伝わる方が返信率が高い
- ページが増えるほど、事実の捏造が混入するリスクが増える
- 作る時間が増えるのに、成約率は上がらない

**複数ページに拡張するのは、契約が決まってから。** そこで初めて下の構成にする。

### 契約後の複数ページ構成

```
index.html        トップ
  1. ヒーロー（キャッチ + 電話CTA）
  2. こんなお困りごとはありませんか（症状/困りごとリスト）
  3. サービス概要（3つ、カード型）→ 詳しくは services.html
  4. 選ばれる理由（★Googleの評価と口コミのみを根拠にする）
  5. お客様の声（渡された口コミの抜粋のみ）
  6. アクセス・営業時間
  7. お問い合わせCTA
  8. フッター（ミセバの注記）

services.html     サービス詳細 + CTA + フッター
about.html        お店について + アクセス + CTA + フッター
contact.html      お問い合わせ（電話 + フォーム）+ フッター
```

- 各下層ページの上部に「← トップに戻る」リンクを置く
- ナビゲーションは全ページ共通
- CTAセクションとフッターは全ページで同一のHTMLを使う

---

## 5. 作業の進め方（段階実行）★重要★

**一度に全部作らない。** 必ず次の段階に分け、各段階の完了を確認してから次へ進む。
一気に作ると品質が落ちる。

| STEP | やること | 完了の判定 |
|---|---|---|
| **1〜3. 骨組み・デザイン・画像** | `scripts/build_site.py <slug>` で一括生成。業種カラー・固定CTAバー・Unsplash画像・アニメーションが入る | 「未置換のプレースホルダー」警告が出ていない |
| **4. 文言** | 渡された実データに差し替え。§1-1 の禁止事項を再チェック | 捏造した事実がゼロ |
| **5. 最終チェック** | SEO / アクセシビリティ / レスポンシブ / リンク切れ | チェックリスト全項目OK |

STEP1〜3をスクリプトに任せているのは、この3段階が機械的で、毎回同じ結果になるべきだから。
**判断が要るのは STEP4 と STEP5。ここは手を抜かない。**

### STEP5 のチェックリスト

- [ ] `<meta name="robots" content="noindex, nofollow">` があるか
- [ ] `meta description` と OGPタグがあるか
- [ ] フッターにミセバの注記があるか
- [ ] 全画像に `alt` があるか
- [ ] 見出し階層（h1→h2→h3）が飛んでいないか
- [ ] 320px / 768px / 1440px でレイアウトが崩れないか
- [ ] `tel:` リンクが**その店の**電話番号を指しているか
- [ ] **店名・住所が別の店のものと入れ替わっていないか**（最悪の事故）
- [ ] **§1-1 の禁止事項（創業年・実績・資格・料金）が1つも書かれていないか**

---

## 6. フォルダのルール

```
prospects/<slug>/       1店舗 = 1フォルダ。他の店のファイルを混ぜない
  info.json             店舗情報（入力）
  site/                 生成したサイト（index.html など）
  outreach.md           送信文面と送信記録
docs/p/<slug>/          公開用。GitHub Pages がここを配信する
templates/base/         雛形。直接編集しない（コピーして使う）
```

**作業対象の店のフォルダだけを読むこと。** 他の店のフォルダを参照すると、
別の店の情報が混入する事故が起きます。

---

## 7. やってはいけないこと

- `templates/base/` を直接書き換える（雛形が壊れる）
- 他の店のフォルダのファイルを読んで流用する（情報混入事故）
- `prospects/` の中身を GitHub に push する（`.gitignore` 済み。個人情報保護のため）
- 予算・課金が発生するAPIを勝手に使う（**ミセバは無課金運用が前提**）
